/* eslint-disable */
const LayoutReducer = (state, action) => {
  switch (action.type) {
    case 'TOGGLE_SIDEBAR':
    // return toggleSidebar(state);

    default:
      return state;
  }
};

export default LayoutReducer;
